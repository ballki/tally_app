<%= form_for @visit, html: { :class => "form-inline",:role => "form" } do |f|%>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>
      <ul>
      <% @visit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
    <% end %>

    <div class="text-center">
      <div class="button-group">
        <input type="email" name="email" placeholder="Please enter your email address" class="btn form-control" id="exampleInputEmail1" >
        <%= f.submit 'GO', :class => 'btn btn-default' %>
      </div>
    </div>
</form>
<% end %>
